import {useState} from 'react';
import {api} from './api';
export default function DeleteCommunity({server,onDeleted}:{server:any,onDeleted:()=>Promise<void>}){const [open,setOpen]=useState(false),[name,setName]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <section className="delete-community"><h3>Excluir comunidade</h3><p>Somente o dono pode excluir. Isso apaga canais, mensagens, eventos, convites e cargos desta comunidade para todos, e encerra as chamadas nela.</p>{!open?<button className="danger" onClick={()=>setOpen(true)}>Excluir esta comunidade</button>:<form className="feature-stack" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{await api('/servers/'+server.id,'DELETE',{name});await onDeleted();}catch(e:any){setError(e.message);setBusy(false);}}}><label>Digite o nome da comunidade para confirmar<strong>{server.name}</strong><input aria-label="Confirmar nome da comunidade" value={name} onChange={e=>setName(e.target.value)} autoComplete="off" maxLength={60}/></label><button className="danger-solid" disabled={busy||name!==server.name}>{busy?'Excluindo…':'Excluir comunidade permanentemente'}</button><button type="button" disabled={busy} onClick={()=>{setOpen(false);setName('');}}>Cancelar exclusão</button>{error&&<p role="alert" className="error">{error}</p>}</form>}</section>;}

