import { z } from 'zod';
export const username = z.string().trim().toLowerCase().regex(/^[a-z0-9_]{3,24}$/, 'Use 3–24 letras, números ou _');
export const credentials = z.object({ username, password: z.string().min(10).max(128) });
export const profile = z.object({ display_name: z.string().trim().min(1).max(50), bio: z.string().max(300), status: z.enum(['online','away','busy','invisible']), dm_policy: z.enum(['everyone','friends','nobody']), read_receipts: z.boolean(), avatar_id: z.string().uuid().nullable().optional(), banner_id: z.string().uuid().nullable().optional(), accent_color: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional(), pronouns: z.string().trim().max(32).optional(), custom_status: z.string().trim().max(100).optional(), theme:z.enum(['nexo','forest','ocean','rose','midnight']).optional(), name_color:z.string().regex(/^#[0-9a-fA-F]{6}$/).optional(), banner_style:z.enum(['orbits','stripes','dots','plain']).optional() });
export const messageInput = z.object({ body: z.string().trim().max(4000).default(''), reply_id: z.string().uuid().nullable().optional(), sticker_id:z.string().uuid().nullable().optional(), attachment_id: z.string().uuid().nullable().optional() }).refine(x => x.body.length || x.attachment_id || x.sticker_id, 'Mensagem vazia');
export const roomInput = z.object({ name: z.string().trim().min(1).max(60), kind: z.enum(['dm','group','text','voice','temporary']), users: z.array(username).max(15).default([]), server_id: z.string().uuid().optional(), category_id: z.string().uuid().nullable().optional() });
export const permissions = ['manage_channels','manage_members','manage_messages','manage_roles','invite','send'] as const;

